spring:
  ai:
    mcp:
      server:
        name: MCP-Slide-Parser
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
  # stdio
  main:
    web-application-type: none
    banner-mode: off
